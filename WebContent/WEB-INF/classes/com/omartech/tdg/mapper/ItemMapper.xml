<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.omartech.tdg.mapper.ItemMapper">
    <insert id="insertItem" parameterType="Item" useGeneratedKeys="true">
        insert into item(
        	sku, name, nameInChinese, featureJson, image, retailPrice, promotionPrice,
        	promotionTime, wholePrice, minimumQuantity, maximumAcceptQuantity,
        	availableQuantity, safeStock, productId, categoryId, sellerId
        )values(
        	#{sku}, #{name}, #{nameInChinese}, #{featureJson}, #{image}, #{retailPrice},#{promotionPrice},
        	#{promotionTime},#{wholePrice}, #{minimumQuantity},#{maximumAcceptQuantity},
        	#{availableQuantity},#{safeStock}, #{productId}, #{categoryId}, #{sellerId}
        )
    	<selectKey keyProperty="id" resultType="long" order="AFTER">  
	        SELECT LAST_INSERT_ID()
	    </selectKey> 
    </insert>
    <update id="updateDefaultSku" parameterType="long">
        update item set sku = #{id} where id = #{id}
    </update>
    <update id="updateSku" parameterType="Item">
        update item set sku = #{sku} where id = #{id}
    </update>
    <select id="getItemById" parameterType="long" resultType="Item">
        select * from item where id = #{id}
    </select>
        <select id="getItemBySku" parameterType="long" resultType="Item">
        select * from item where sku = #{sku}
    </select>
    <select id="getItemListByProductId" parameterType="long" resultType="Item">
        select * from item where productId = #{productId}
    </select>
</mapper>