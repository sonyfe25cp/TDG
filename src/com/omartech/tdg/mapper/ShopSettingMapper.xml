<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.omartech.tdg.mapper.ShopSettingMapper">
	<insert id="insertShopSetting" parameterType="ShopSetting">
		insert into shopsetting(
				sellerId, shippingCountry, shippingPromiseDays, description, defaultCoinage)
			values(#{sellerId}, #{shippingCountry}, #{shippingPromiseDays}, #{description}, #{defaultCoinage})
	</insert>
	<select id="getShopSettingById" parameterType="int" resultType="Coinage">
		select * from coinage where id = #{id}
	</select>
	
	<select id="getShopSettingBySellerId" parameterType="int" resultType="Coinage">
		select * from coinage where sellerId = #{sellerId}
	</select>
	
	<update id="updateShopSetting">
		update shopsetting set 
			shippingCountry = #{shippingCountry} and
			shippingPromiseDays = #{shippingPromiseDays} and
			description = #{description} and
			defaultCoinage = #{defaultCoinage}
		where id = #{id}
	</update>
	<update id="translateShopSetting">
		update shopsetting set descriptionInChinese = #{descriptionInChinese} where id = #{id}
	</update>
</mapper>