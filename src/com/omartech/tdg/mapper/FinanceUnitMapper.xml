<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.omartech.tdg.mapper.FinanceUnitMapper">
	<insert id="insert" parameterType="FinanceUnit">
		insert into financeUnit(sellerId, customerId, orderId, orderMoney, platformMoney, createAt, financeType, status)
		values(
			#{sellerId}, #{customerId}, #{orderId}, #{orderMoney}, #{platformMoney}, #{createAt}, #{financeType}, #{status}
		)
	</insert>
	<update id="update" parameterType="FinanceUnit">
		update financeUnit set finishAt = #{finishAt}, status = #{status}, comment = #{comment} where id = #{id}
	</update>
	<select id="getFinanceBySellerIdAndMonthByPage" resultType="FinanceUnit">
		select * from financeUnit where sellerId = #{sellerId} and <![CDATA[ createAt > begin and createAt < end ]]> limit #{page.numBegin}, #{page.pageSize}
	</select>
	<select id="getFinanceByMonthByPage" resultType="FinanceUnit">
		select * from financeUnit where <![CDATA[ createAt > begin and createAt < end ]]> limit #{page.numBegin}, #{page.pageSize}
	</select>
</mapper>